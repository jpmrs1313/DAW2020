extends layout

block content
    .w3-card-4
      header.w3-container.w3-teal
        h3.w3-left Editar informações do aluno #{info.numero} 
    form(action='/students/'+info.numero, method='POST')
            br
            | Nome: 
            input(type='text', name='nome', value=info.nome)
            br
            br
            | Link do Git: 
            input(type='text', name='git', value=info.git)
            br
            br
            input(type='submit', value='Alterar')


